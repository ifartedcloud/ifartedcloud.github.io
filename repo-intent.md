# Repo intent — ifartedcloud.github.io

**Early-stage personal project — purpose not yet documented beyond the name and stack.**

## What this repo is

A TypeScript + Vite web app, scaffolded with an `AGENTS.md`/`CLAUDE.md` agent-instruction
pair, but with no README content beyond the title and no repository description set on
GitHub. What `ifartedcloud` is actually for isn't written down anywhere in the repo as
of this writing (2026-08-26).

## Shape

- Standard Vite + TypeScript layout (`src/`, `public/`, `index.html`, `vite.config.ts`)
- `.claude/`, `AGENTS.md`, `CLAUDE.md` — agent tooling is already set up even though the
  product itself isn't described yet

## Status

Early / scaffold. **This section is a placeholder, not a real intent statement** —
replace it with the actual purpose the next time work happens on this repo. Don't let
the name stand in for a description.
